name: dev_ecosystem
version: "1.0.0"
config-version: 2

profile: dev_ecosystem

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dev_ecosystem:
    staging:
      +materialized: view
    marts:
      +materialized: table

